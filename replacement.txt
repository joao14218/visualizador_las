"        <section className={`grid gap-6 transition-all duration-300 ${\n          showSidebar \n            ? \"grid-cols-1 lg:grid-cols-[300px_minmax(0,1fr)]\" \n            : \"grid-cols-1\"\n        }`}>\n\n          {/* Sidebar */}\n          {showSidebar && (\n            <aside className=\"space-y-6\">\n\n              {/* Arquivos */}\n              <Card className=\"rounded-3xl border border-slate-800 bg-slate-900/80 text-slate-100 shadow-xl\">\n                <CardContent className=\"p-5\">\n                  <div className=\"mb-4 flex items-center gap-3\">\n                    <FileText className=\"text-cyan-300\" />\n                    <h2 className=\"font-semibold\">Arquivos ({files.length})</h2>\n                  </div>\n\n                  {files.length === 0 ? (\n                    <p className=\"text-sm text-slate-500 mb-4\">Nenhum arquivo carregado.</p>\n                  ) : (\n                    <div className=\"space-y-2 mb-4\">\n                      {files.map(f => (\n                        <div\n                          key={f.id}\n                          onClick={() => setActiveFileId(f.id)}\n                          className={\"flex items-center justify-between rounded-2xl px-3 py-2 text-sm cursor-pointer transition \" +\n                            (f.id === activeFileId ? \"bg-cyan-300/10 border border-cyan-300\" : \"bg-slate-800 border border-transparent hover:border-slate-600\")}\n                        >\n                          <div className=\"min-w-0\">\n                            <p className=\"truncate font-medium text-slate-200\">{f.name}</p>\n                            <p className=\"text-xs text-slate-500\">\n                              {f.parsed.data.length} amostras · {Math.max(0, f.parsed.curves.length - 1)} curvas\n                            </p>\n                          </div>\n                          <div className=\"flex items-center gap-1 flex-shrink-0 ml-2\">\n                            {!wellCoords[f.id] && (\n                              
<truncated 13242 bytes>